# Backend Configuration
BACKEND_URL=http://localhost
BACKEND_PORT=8080

# Rig Identification (must match backend configuration)
# Valid values: 1, 2, 3, etc. (must be within NUMBER_OF_RIGS configured in backend)
RIG_ID=1

# Assetto Corsa Server Configuration
# Server IP address or hostname
AC_SERVER_IP=192.168.1.100
# Server UDP port (default: 9600)
AC_SERVER_PORT=9600
# Server HTTP port (required for Content Manager)
AC_SERVER_HTTP_PORT=8081
# Server name (optional, for display purposes)
AC_SERVER_NAME=Race Server
# Car model ID to use when joining
AC_CAR_ID=ks_mercedes_amg_gt3
# Steam ID of the account on this rig
AC_STEAM_ID=76561198000000000
