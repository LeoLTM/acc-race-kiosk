# Backend Configuration
BACKEND_URL=http://localhost
BACKEND_PORT=8080

# Rig Identification (must match backend configuration)
# Valid values: 1, 2, 3, etc. (must be within NUMBER_OF_RIGS configured in backend)
RIG_ID=1
